services:
  - type: web
    name: tquiz
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    region: singapore
    plan: free
